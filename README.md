Vim-config
==========

My Vim setup

# Setup
* Clone the repo into .vim:

    git clone git@github.com:th3james/vim-config.git
    mv vim-config ~/.vim

* Symlink vimrc

    ln -s ~/.vim/vimrc ~/.vimrc
